# NOTES
For documentation add a JSON that has original functions names, renamed names, and comments. in per function routine a python script decides which function is next, then the AI just annotates with the json being the focus, the .mds are extra.

actually, try just using a "Documented-1" tag
have folder for scripts in workspace, they are all exported/aliased, so you can for example aipiler-list-functions.
